name: DEWEY-local
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.13
  - pip
  - pip:
      - daylily-cognito==0.1.32
      - daylily-tapdb==3.0.11
      - playwright>=1.42.0
      - metapub
      - pytest>=8.0.0
      - pytest-playwright>=0.4.4
